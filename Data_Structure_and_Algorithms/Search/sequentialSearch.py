def sequentialSearch(a_list, item):
    pos = 0
    found = False

    while pos < len(a_list) and not found:
        if a_list[post] == tiem:
            found = True
        else:
            pos += 1

    return found
